// 1. Copy this file to Secrets.swift (in the same directory)
// 2. Replace the placeholder with your Sezzle sandbox public key
// 3. Secrets.swift is gitignored — your key stays out of version control
enum Secrets {
    static let sezzlePublicKey = "YOUR_SANDBOX_PUBLIC_KEY"
}
// Get your key at: https://dashboard.sezzle.com → Settings → API Keys
